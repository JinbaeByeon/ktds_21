package market;

/**
 * 판매자
 * 
 * @author User
 *
 */
public class Seller extends Trader {
	/**
	 * 판매
	 * (buyer에게 amount만큼 판매)
	 * @param buyer 구매자
	 * @param amount 주문개수
	 */
	public boolean sell(Buyer buyer, int amount) {
		buyer.pay(this,amount);
	}
}
