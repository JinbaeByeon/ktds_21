package market;

/**
 * 판매자
 * 
 * @author User
 *
 */
public class Seller extends Trader {
	/**
	 * 판매
	 * (buyer에게 amount만큼 판매)
	 * @param amount 주문개수
	 */
	public int sell(int amount) {
		
		if(stock < amount) {
			return 0;
		}
		stock-=amount;
		return amount;		
	}
	
	public void show() {
		System.out.print("[판매자]");
		super.show();
	}
}
